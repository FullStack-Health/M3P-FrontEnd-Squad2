import { DateFormatInPipe } from './date-format-in.pipe';

describe('DateFormatterInPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatInPipe();
    expect(pipe).toBeTruthy();
  });
});
