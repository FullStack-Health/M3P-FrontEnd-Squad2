import { DateFormatOutPipe } from './date-format-out.pipe';

describe('DateFormatOutPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatOutPipe();
    expect(pipe).toBeTruthy();
  });
});
